<Window x:Class="ClickUpOverlay.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ClickUpOverlay - Logs" Height="550" Width="850"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundDark}"
        FontFamily="Segoe UI">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" Text="API Response Logs" 
                   FontSize="20" FontWeight="SemiBold" 
                   Foreground="{StaticResource TextPrimary}"
                   Margin="0,0,0,15"/>
        
        <Border Grid.Row="1" Background="#0D1117" 
                BorderBrush="{StaticResource BorderColor}" 
                BorderThickness="1" 
                CornerRadius="4">
            <ScrollViewer VerticalScrollBarVisibility="Auto" 
                          HorizontalScrollBarVisibility="Auto"
                          Background="Transparent">
                <TextBox x:Name="LogTextBox" IsReadOnly="True" 
                         FontFamily="Consolas" FontSize="12"
                         TextWrapping="NoWrap" AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Background="Transparent" 
                         Foreground="#7EE787"
                         BorderThickness="0"
                         Padding="12,10"
                         CaretBrush="#7EE787"/>
            </ScrollViewer>
        </Border>
        
        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="ClearButton" Grid.Column="0" Content="Clear Logs" 
                    Click="ClearButton_Click" Height="38" FontSize="13"/>
            <Button x:Name="CloseButton" Grid.Column="2" Content="Close" 
                    Click="CloseButton_Click" Height="38" FontSize="13" FontWeight="SemiBold"
                    Width="120" IsDefault="True"
                    Background="{StaticResource AccentColor}"
                    Foreground="White"
                    BorderThickness="0">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Setter Property="Background" Value="{StaticResource AccentColor}"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#FF3333"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>
    </Grid>
</Window>
